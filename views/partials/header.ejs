<!DOCTYPE html>
<html>
  <head>
    <title>TickeTac</title>
    <!-- fontawesome -->
    <script src="https://kit.fontawesome.com/47349f3626.js" crossorigin="anonymous"></script>
    <!-- bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
      <!--CSS -->
    <link rel='stylesheet' href='/stylesheets/style.css' />

  </head>
  <body>
    
      
      <nav class="navbar navbar-light bg-light">
        <div class="container-fluid">
          <% if(isAuth) { %>
          <a class="navbar-brand" href="/home">
            <% } else { %>
            <a class="navbar-brand" href="/">
            <% } %>
            <img src="/images/train.png" alt="" width="30" height="24" class="d-inline-block align-text-top">
            Ticketac
          </a>
          <div>
            <% if(isAuth) { %>
              <a class="navbar-brand" href="/basket">
              <button type="button" class="btn position-relative basket">
                <i class="fas fa-shopping-basket"></i>
                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                  <%=compteurPanier%>
                  <span class="visually-hidden">number of tickets</span>
                </span>
              </button>
              </a>            
              <a class="navbar-brand" href="/logout">
                <span id="connection">Logout</span>
              </a>
              <a class="navbar-brand" href="/MyLastTrips">
                My last trips
              </a>
            <% } %>
          </div>
        </div>
      </nav>
      
    <div class="container-fluid">